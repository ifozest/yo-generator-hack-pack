language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-hack-pack' ]; then cd .. && eval "mv $currentfolder generator-hack-pack" && cd generator-hack-pack; fi

